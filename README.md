# hookah-db

Репозиторий схемы PostgreSQL для системы управления кальянной. Здесь живут миграции, сиды и локальная инфраструктура базы.

## Доменное разбиение

- `catalog` — бренды, линейки и вкусы табака;
- `inventory` — партии и движения по складу;
- `sales` — заказы и позиции;
- `support` — общие перечисления и служебные типы.

## Файлы

- `compose.yaml` — локальный PostgreSQL-контур;
- `migrations` — версия схемы и структура БД;
- `seeds` — демо-данные для локальной разработки.

## Как запускать локально

```bash
copy .env.example .env
docker compose up -d
```

После старта контейнера миграции и сиды можно накатывать вручную через `psql` или через отдельный migration runner, когда мы его добавим.

## Ближайшие шаги

- Добавить таблицы пользователей и ролей.
- Ввести аудит по изменениям складских остатков.
- Определить стратегию rollback для production-миграций.
